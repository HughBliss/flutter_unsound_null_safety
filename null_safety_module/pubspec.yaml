name: 'null_safety_module'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  get: ^4.1.4
  flutter:
    sdk: flutter
  unsound_null_safety:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
